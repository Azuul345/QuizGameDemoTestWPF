<UserControl x:Class="QuizGame.Views.ResultsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuizGame.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="520" >

            <!-- Score header -->
            <TextBlock Text="{Binding ScoreText}"
                 FontSize="22" FontWeight="Bold"
                 HorizontalAlignment="Center" Margin="0,0,0,16"/>

            <!-- Toggle to show wrong answers -->
            <Expander Header="Show questions you got wrong" IsExpanded="False" Margin="0,0,0,12">
                <ItemsControl ItemsSource="{Binding WrongAnswers}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="8" Margin="0,0,0,8" BorderBrush="LightGray" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="{Binding Statement}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding UserAnswer}"  Foreground="IndianRed"/>
                                    <TextBlock Text="{Binding CorrectAnswer}" Foreground="ForestGreen"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Expander>

            <!-- Actions -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" >
                <Button Content="Play again" Width="140" Height="36" Margin="0,0,8,0"
                Click="PlayAgain_Click"/>
                <Button Content="Main menu" Width="140" Height="36"
                Click="Menu_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
